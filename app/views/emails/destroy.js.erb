var e = $('#email_<%= @email.id %>');

function animEnded() {
  e.css("display", "none");
}

e.one('webkitAnimationEnd mozAnimationEnd MSAnimationEnd oanimationend animationend', animEnded);
e.addClass('animated bounceOutLeft');